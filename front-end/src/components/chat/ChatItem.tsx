import React from 'react';
import styled from 'styled-components';


interface Props {
  title: string;
};


const ChatItem : React.FC<Props> = ({ title }) => {
  return (<div></div>);
}

export default ChatItem;